#!/usr/bin/env zsh
# superpowers — surgical execution launcher
# Responsibilities: define default models, configure Claude behaviour,
#                   export CLAUDE_ROUTER_MODEL, invoke claude_router.
# The router handles all model validation, routing, caching, and preset work.

_SUPERPOWERS_DIR="${${(%):-%x}:A:h}"

# ── Default model list ────────────────────────────────────────────────────────

typeset -a _SUPERPOWERS_DEFAULTS
_SUPERPOWERS_DEFAULTS=(
    "openrouter/auto"
    "cohere/north-mini-code"
    "deepseek/deepseek-v4-flash"
    "xiaomi/mimo-v2.5-pro"
    "deepseek/deepseek-v4-pro"
)

# ── Model selection ───────────────────────────────────────────────────────────

export CLAUDE_ROUTER_DEFAULT_MODELS="${(F)_SUPERPOWERS_DEFAULTS}"
export CLAUDE_ROUTER_MODEL="__pick__"

# ── Load and run the router ───────────────────────────────────────────────────

source "${_SUPERPOWERS_DIR}/router/router_engine.zsh"
claude_router || return 1

# ── Claude behaviour ──────────────────────────────────────────────────────────

unset CLAUDE_CODE_DISABLE_ADVISOR_TOOL
unset CLAUDE_CODE_PLAN_MODE_INTERVIEW_PHASE
unset SUPERPOWERS_MODIFIER

# ── Launch ────────────────────────────────────────────────────────────────────

exec claude "$@"
